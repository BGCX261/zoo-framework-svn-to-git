/*
 * Video Player Library
 * 
 * VideoPlayer.add("videos/video1.flv", container:MovieClip, {
 * autoplay:Boolean,
 * seek:int,
 * format:String,
 * volume:int,
 * streaming:Boolean,
 * onFinishVideo:Function,
 * onInitVideo:Function,
 * onResize:String,
 * id:String
 * })
 * 
 * VideoPlayer.play(id:String);
 * VideoPlayer.pause(id:String);
 * VideoPlayer.stop(id:String);
 * VideoPlayer.fullscreen(id:String);
 * VideoPlayer.seek(id:String);
 * VideoPlayer.volume(id:String);
 * VideoPlayer.gotoCuePoint(id:String, {cuePointName:String});
 * 
 * 
 * 
 */

package com.zoo.video {

	/**
	 * @author marcelo duende
	 */
	public class VideoPlayer {
	}
}
